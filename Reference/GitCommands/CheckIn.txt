git checkout -b "To Create New Branch"
git checkout "To switch to existing Branch"
git pull --all
git merge develop
git status
git add .
git status
git commit -m "Initial checkin"
git push -u origin "Branch name"

---------

git config --global user.name “Rohan Chauhan”
git config --global user.email “rohan.chauhan.in18@gmail.com”
git config --list
git config remote.origin.url